import { PasswordGeratorCard } from './components/PasswordGeneratorCard/PasswordGeneratorCard';
import s from "./style.module.css"

function App() {
  return (
    <div className={s.root}>
      <PasswordGeratorCard />
    </div>
  );
}

export default App;
